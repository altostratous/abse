#include<iostream>
#include<algorithm>
#include "ds.h"
#include<vector>
using namespace std;
using namespace ds;

/*
bool checkCount (wanalysis* g, wanalysis* a) { return (g->getCount() < a->getcount()); }
bool checkWord (wanalysis* g, wanalysis* a) { return (g-> getword()< a->getword)(); }
void sort(vector<wanalysis>& was)
{
	sort(was.begin(),was.end(), checkCount)
	sort(was.begin(),was.end(), checkWord)
}*/
